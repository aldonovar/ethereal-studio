# Public browser configuration. Copy this file to the ignored `.env.local`.
VITE_SUPABASE_URL=https://xnmkoybfuyivmiuckpxs.supabase.co
VITE_SUPABASE_ANON_KEY=

# Desktop reuses this same project-scoped Supabase social provider through the
# protected HTTPS bridge. It does not require a separate OAuth client or secret.
